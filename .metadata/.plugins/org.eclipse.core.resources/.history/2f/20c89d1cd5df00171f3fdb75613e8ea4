package 引论.本章讨论内容;

/**
 * 设有一组N个数而要确定其中第K个最大值 除了冒泡 书中提到可以把前K个元素读进一个数组并（以递减的顺序）进行排序，
 * 接着将剩下的元素在逐个读入，当新元素读入时，如果小于第K个元素则忽略， 否则放到正确的位置，
 * 同时将数组中的一个元素挤出数组， 当算法终结时位于第
 * K个位置的元素作为答案返回;
 * {1,2,3,4,5,6,7,8}的第2大的是6索引的7
 * {84,21,4,5,1,54,68,524,58,454,6584,534,586,} 的第2小的是2索引的4
 * @author Administrator
 *
 */
public class DemoOne {
	public static void main(String[] args) {
		int k = 1;
		int[] is = new int[30000];
		for (int i = 0; i < is.length; i++) {
			is[i] = RandomNumber(1, 200);
		}
		// for (int i = 0; i < is.length; i++) {
		// if (is[i]==199) {
		// System.out.println("is中的"+i+"座标是200");
		// break;
		// }
		// }

		Demo(k, is);
	}

	/**
	 * 
	 * @param k
	 *            第K个大数
	 * @param is
	 *            数组
	 */
	public static void Demo(int k, int[] is) {
		int max = 0;
		for (int i = 0; i < is.length; i++) {
			for (int j = i+1; j < is.length; j++) {
				if (is[i]>is[j]) {
					max=is[i];
				}
			}
		}
		System.out.println(max);
		for (int i = 0; i < is.length; i++) {
			if (max<is[i]) {
				System.out.println(is[i]+"AAA");
			}
		}
//		int myIs [] = new int[];
		
	}

	/**
	 * 包含start 不包含end
	 * 
	 * @param start
	 *            初始值
	 * @param end
	 *            结束值
	 * @return 每次产生的随机数
	 */
	public static int RandomNumber(int start, int end) {
		// 首先声明这个是强制转换成int类型的数值
		// 调用lang包内的Math中的random的方法 她可以返回一个为伪随机的带正号的大于等于0.0小于0.1的double数值
		// 最后加上start是为了在Math.random()获取到0 之后 也可以保证return的数值大于等于start
		return (int) (Math.random() * (end - start) + start);
	}
}
