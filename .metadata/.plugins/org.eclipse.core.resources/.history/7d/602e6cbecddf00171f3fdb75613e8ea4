package 引论.本章讨论内容;

/**
 * 设有一组N个数而要确定其中第K个最大值 除了冒泡 书中提到可以把前K个元素读进一个数组并（以递减的顺序）进行排序，
 * 接着将剩下的元素在逐个读入，当新元素读入时，如果小于第K个元素则忽略， 否则放到正确的位置，同时将数组中的一个元素挤出数组， 当算法终结时位于第
 * K个位置的元素作为答案返回;
 * 
 * @author Administrator
 *
 */
public class DemoOne {
	public static void main(String[] args) {

		int k = 1500000;
		int[] is = new int[3000000];
		for (int i = 0; i < is.length; i++) {
			RandomNumber(1, 2);
		}

		Demo(k, is);
	}

	/**
	 * 
	 * @param k
	 *            第K个数
	 * @param is
	 *            数组
	 */
	public static void Demo(int k, int[] is) {

	}

	/**
	 * 包含start 不包含end
	 * 
	 * @param start
	 *            初始
	 * @param end
	 *            结束
	 * @return 每次产生的随机数
	 */
	public static int RandomNumber(int start, int end) {
		int number = (int) (Math.random() * (end - start + 1) + start);
		return number;
	}
}
